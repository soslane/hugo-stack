{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build"
    }
  ],
  "build": {
    "env": {
      "GO_VERSION": "1.17"
    }
  },
  "scripts": {
    "build": "curl -LO https://golang.org/dl/go1.17.linux-amd64.tar.gz && tar -C /usr/local -xzf go1.17.linux-amd64.tar.gz && export PATH=$PATH:/usr/local/go/bin && hugo --gc --minify"
  }
}
